import Link from 'next/link'; import { ArrowLeft, Boxes, ExternalLink, Route } from 'lucide-react'; import { plugins } from '@/lib/generated-plugins'; import { titleCase } from '@/lib/utils'; import { Card,CardDescription,CardHeader,CardTitle } from '@/components/ui/card'; import { Button } from '@/components/ui/button';
export default async function Category({params}:{params:Promise<{category:string}>}){const {category}=await params; const list=plugins.filter(p=>p.category===category); return <section className="container py-12 md:py-16"><Button asChild variant="outline" className="mb-6 rounded-full"><Link href="/docs"><ArrowLeft className="h-4 w-4"/>Kembali ke Docs</Link></Button><div data-aos="modern-up" className="rounded-[2rem] border bg-card p-6 shadow-2xl shadow-primary/5 md:p-10 soft-grid"><div className="mb-4 grid h-14 w-14 place-items-center rounded-2xl bg-primary/10 text-primary"><Boxes className="h-7 w-7"/></div><h1 className="text-4xl md:text-6xl">{titleCase(category)}</h1><p className="mt-3 text-muted-foreground">{list.length} endpoint tersedia di kategori ini.</p></div><div className="mt-8 grid gap-5 md:grid-cols-2">{list.map((p,i)=><Link data-aos="modern-up" data-aos-delay={i*70} key={p.slug} href={`/docs/${p.category}/${p.slug}`}><Card className="modern-card h-full"><CardHeader><div className="mb-4 flex items-center justify-between"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-primary/10 text-primary"><Route className="h-5 w-5"/></span><ExternalLink className="h-4 w-4 text-muted-foreground"/></div><CardTitle>{p.title}</CardTitle><CardDescription>{p.description}</CardDescription></CardHeader></Card></Link>)}</div></section>}
