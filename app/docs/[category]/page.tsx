import Link from 'next/link'; import { plugins } from '@/lib/generated-plugins'; import { titleCase } from '@/lib/utils'; import { Card,CardHeader,CardTitle,CardDescription } from '@/components/ui/card'
export default async function Category({params}:{params:Promise<{category:string}>}){const {category}=await params; const list=plugins.filter(p=>p.category===category); return <section className="container py-12"><h1 className="text-4xl md:text-6xl">{titleCase(category)}</h1><p className="mt-3 text-muted-foreground">{list.length} endpoint tersedia.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{list.map(p=><Link data-aos="fade-up" key={p.slug} href={`/docs/${p.category}/${p.slug}`}><Card className="h-full transition hover:-translate-y-1 hover:border-primary"><CardHeader><CardTitle>{p.title}</CardTitle><CardDescription>{p.description}</CardDescription></CardHeader></Card></Link>)}</div></section>}
