'use client'; import * as Dialog from '@radix-ui/react-dialog'; import { X } from 'lucide-react'; import { cn } from '@/lib/utils'
export const Sheet=Dialog.Root; export const SheetTrigger=Dialog.Trigger; export const SheetClose=Dialog.Close;
export const SheetContent=({className,children,...p}:React.ComponentProps<typeof Dialog.Content>)=><Dialog.Portal><Dialog.Overlay className="fixed inset-0 z-50 bg-background/50 backdrop-blur-md"/><Dialog.Content className={cn('fixed left-0 top-0 z-50 h-full w-80 max-w-[86vw] border-r bg-card p-6 shadow-lg animate-in slide-in-from-left',className)} {...p}>{children}<Dialog.Close className="absolute right-4 top-4"><X className="h-5 w-5"/></Dialog.Close></Dialog.Content></Dialog.Portal>
