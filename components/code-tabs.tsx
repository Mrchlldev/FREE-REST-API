import { Tabs,TabsContent,TabsList,TabsTrigger } from '@/components/ui/tabs'
export function samples(url:string){return {curl:`curl \"${url}\"`,javascript:`const res = await fetch('${url}')\nconst data = await res.json()\nconsole.log(data)`,python:`import requests\nres = requests.get('${url}')\nprint(res.json())`,go:`package main\nimport (\"fmt\"; \"net/http\"; \"io\")\nfunc main(){\n  res,_ := http.Get(\"${url}\")\n  body,_ := io.ReadAll(res.Body)\n  fmt.Println(string(body))\n}`,php:`<?php\n$res = file_get_contents('${url}');\necho $res;`}}
export function CodeTabs({url}:{url:string}){const s=samples(url); return <Tabs defaultValue="javascript"><TabsList className="flex flex-wrap h-auto"><TabsTrigger value="curl">cURL</TabsTrigger><TabsTrigger value="javascript">Javascript</TabsTrigger><TabsTrigger value="python">Python</TabsTrigger><TabsTrigger value="go">Go</TabsTrigger><TabsTrigger value="php">PHP</TabsTrigger></TabsList>{Object.entries(s).map(([k,v])=><TabsContent key={k} value={k}><pre className="code">{v}</pre></TabsContent>)}</Tabs>}
