'use client'
import { Copy, Check } from 'lucide-react'; import { useState } from 'react'; import { Button } from '@/components/ui/button';
export function CodeBlock({code,title='Code'}:{code:string;title?:string}){const [ok,setOk]=useState(false); const copy=async()=>{await navigator.clipboard.writeText(code); setOk(true); setTimeout(()=>setOk(false),1200)}; const lines=code.split('\n'); return <div className="overflow-hidden rounded-2xl border bg-card"><div className="flex items-center justify-between border-b bg-secondary/60 px-4 py-3"><p className="text-sm font-bold">{title}</p><Button size="sm" variant="outline" className="rounded-full" onClick={copy}>{ok?<Check className="h-4 w-4"/>:<Copy className="h-4 w-4"/>}{ok?'Copied':'Copy'}</Button></div><pre className="code-lines overflow-x-auto p-4 text-sm">{lines.map((l,i)=><span className="line" key={i}>{l || ' '}</span>)}</pre></div>}
