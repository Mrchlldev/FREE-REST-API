'use client'
import Link from 'next/link'; import { Menu, Boxes } from 'lucide-react'; import { Button } from '@/components/ui/button'; import { Sheet, SheetContent, SheetTrigger } from '@/components/ui/sheet'; import { ThemeToggle } from '@/components/theme-toggle'; import { categories } from '@/lib/generated-plugins'; import { titleCase } from '@/lib/utils';
export function Header(){return <header className="sticky top-0 z-40 border-b bg-background/80 backdrop-blur"><div className="container flex h-16 items-center justify-between"><Link href="/" className="font-title text-2xl font-black">Com1</Link><div className="flex items-center gap-2"><ThemeToggle/><Sheet><SheetTrigger asChild><Button variant="outline" size="icon"><Menu className="h-5 w-5"/></Button></SheetTrigger><SheetContent><div className="mb-8 font-title text-2xl font-black">Com1</div><nav className="space-y-3"><Link className="block rounded-lg px-3 py-2 hover:bg-accent" href="/docs">Docs</Link>{categories.map(c=><Link key={c.slug} href={`/docs/${c.slug}`} className="flex items-center justify-between rounded-lg border p-3 hover:bg-accent"><span className="flex items-center gap-2"><Boxes className="h-4 w-4"/>{titleCase(c.slug)}</span><span className="text-sm text-muted-foreground">{c.count}</span></Link>)}</nav></SheetContent></Sheet></div></div></header>}
